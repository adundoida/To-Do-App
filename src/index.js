import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";


ReactDOM.render(<App />,document.getElementById('root')
);

/*create a component called Action
inside the component create a div
inside the div create a button that reads what to do
export that Action component and import it
inside the App component
*/

/*create a component called Option
inside the component create a div that reads "options component here"
export that option component and import it
inside the App component
*/

/*
create a component called Adoption
inside the component create a div that reads "adoption component here"
export that adoption component and import it
inside the App component

*/
